$('#scratch').wScratchPad({
  size        : 75,          // The size of the brush/scratch.
  bg          : 'https://picsum.photos/id/354/1920/1200',  // Background (image path or hex color).
  fg          : '#000',  // Foreground (image path or hex color).
});
